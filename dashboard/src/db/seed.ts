/** Seed data baseline counts — subtract these to get "new" simulation-generated entities. */
export const SEED_COUNTS: Record<string, number> = {
  customers: 12,
  products: 25,
  orders: 40,
  order_items: 40,
  tickets: 8,
  ticket_messages: 11,
  knowledge_base: 18,
  transactions: 18,
  callbacks: 0,
  channels: 3,
  channel_messages: 0,
};

export const SEED_TICKET_COUNT = 8;
export const SEED_MESSAGE_COUNT = 11;
